# readme of package,totally
# init dirs
# prepare constant files:
	md5sum_0,md5sum_01....
	okay_0,okay01...
# tokenfile = token file
tokenfile is encoded_okaystr_binary
sample(./share/okaies/encoded_okay-11): #client will see 'tokenfile'
	Salted__沿~u佥+齻振梴詴饣Ms傈
above means:'okay' be coded,key file is 11's md5sum.
decode it,run:
	openssl enc -d -aes256 -in tokenfile -kfile ./share/md5s/md5sum_11 -out okay.txt 


init: first run "first.sh"
add new user to editors list.

i////// store mile
stonemile
milestone
which is right,i think the last:milestone.


当前日期: 2015/09/30 周三 
输入新日期: (年月日) 
hp-pc input above some lines.
from master,from rpi,run 'git checkout --orphan saltrepo'
then,in another directory,run 'git clone --bare --branch=saltrepo /home/pi/saltit',get a auto-cream directory,named 'saltrepo.git'

//
then,from remote computer,let us say,it is hp pc,do:
git clone pi@192.168.0.125:~/saltit.git'

we got a bare repo and has push-power clients(notice the 's'!)
we can commits from anywhere afterward.


//Thu Oct  8 09:09:02 CST 2015
from raspi(respoty itself),make a comment.


//Thu Oct  8 09:18:21 CST 2015
from raspi,make another comment. 

//当前日期: 2015/10/14 周三 
now pc:alancomputer,want to check if mirror git site can do same as origin.
write above see changes.
输入新日期: (年月日) 
